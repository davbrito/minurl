import * as styles from "./styles.module.css";

const Header = () => {
  return (
    <header className={styles.root}>
      <h1 className={styles.title}>miniurl</h1>
    </header>
  );
};

export default Header;
